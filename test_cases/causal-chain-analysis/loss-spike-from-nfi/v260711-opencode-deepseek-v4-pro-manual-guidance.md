# 因果链分析报告：Slingshot 训练 loss 尖峰

## 初始缺点

loss上升

## 因果链分析

- loss上升
	- 正确类预测概率降低
		- 正确类输出logit低于其他类别logit
			- 存在j≠c满足z_c < z_j
				- z_k = w_k^T·h + b_k（logit由线性分类器产生）：末端（系统设计决策）
				- (w_c-w_j)^T·h + (b_c-b_j)取值为负
					- (w_c-w_j)^T·h取值小于b_j-b_c
						- 向量投影取值减小：×
							- (w_c-w_j)^T·h = ||w_c-w_j||·||h||·cosθ：末端（数学定义）
							- 特征向量h的模长||h||减小
								- SC期间h偏离分类子空间
									- SC期间φ沿W_G方向更新
										- φ的更新梯度∂L/∂φ被W_G方向主导：下方已分析
							- 分类权重差向量模长||w_c-w_j||大：末端（自然现象）
							- cosθ减小
								- h与分类方向(w_c-w_j)的夹角θ增大
									- h的方向偏离分类方向
										- φ的更新方向仅有类别无关信息：下方已分析
					- 模型参数大幅改变
						- Adam参数更新步长Δθ过大：×
							- 基础学习率η过大：末端（用户设定超参数）
							- 一阶矩估计m̂过大：×
								- m_t取值大：×
									- 旧动量β1·m_{t-1}在SC期间衰减趋零：末端（数学极限性质）
									- 新梯度分量(1-β1)·g_t取值大：×
										- 当前步梯度g_t过大
											- p_c在float32下取值降低
												- z_c与最大其他logit差值缩小
													- SC期间特征向量h的分类区分度降低
														- φ的更新方向由W_G主导
															- SC期间特征提取器参数φ在非零梯度下持续更新
																- ∂L/∂h与∂h/∂φ两项均非零
																	- SC期间特征层接收的反向传播梯度∂L/∂h非零：下方已分析
																	- 特征提取器Jacobian ∂h/∂φ非零：末端（自然现象）
															- 特征梯度∂L/∂h中W_G方向分量占主导地位
																- ∂L/∂h中W_G方向分量的幅度增大
																	- 非正确类权重w_i沿-h方向累积位移
																		- w_i在-h方向的分量增大
																			- SC期间每步优化器对w_i施加Δw_i=-η·p_i·h的非零更新
																				- p_i在float32下非零：下方已分析
																				- 优化器学习率η非零：末端（用户设定超参数）
																				- 特征向量h非零：末端（自然现象）
																		- 非正确类权重均值W_G的模长增大
																			- W_G的-h分量增大
																				- W_G = (1/K)Σw_i：末端（系统设计决策）
																				- 各w_i的-h分量累积增大
																					- 各w_i的-h分量每步增大
																						- ∂L/∂w_i = p_i·h得出Δw_i = -η·p_i·h：上方已分析
																- 分类方向的有用梯度信号相对变弱
																	- SC期间正确类对特征梯度∂L/∂h的贡献为零
																		- p_c在float32下取值为1.0：下方已分析
															- SC期间特征层接收的反向传播梯度∂L/∂h非零
																- SC期间非正确类的p_i·w_i项之和非零
																	- 各非正确类softmax概率p_i在float32下非零
																		- exp(z_i-z_c)在float32下可表示为非零值：下方已分析
																	- 各非正确类权重向量w_i在SC期间非零：末端（自然现象）
										- Adam一阶矩系数1-β1取值过大：末端（Adam默认超参数）
								- 偏差校正因子1/(1-β1^t)取值接近1：末端（数学极限性质）
							- Adam自适应缩放因子过大
								- η/(√v̂+ε)取值过大
									- √v̂+ε取值极小
										- Adam的ε参数取值较小：末端（Adam默认超参数）
										- Adam二阶矩v̂极小
											- v_t取值极小：×
												- 旧EMA分量β2·v_{t-1}在SC期间指数衰减：末端（数学极限性质）
												- 新梯度分量(1-β2)·g_t²在SC期间接近零
													- 正确类的当前步梯度g_t接近零：下方已分析
													- β2的EMA系数1-β2非零：末端（Adam默认超参数）
											- 偏差校正因子1/(1-β2^t)取值接近1：末端（数学极限性质）
											- 连续步骤中正确类别梯度接近零
												- 正确类softmax梯度被舍入为零
													- ∂L/∂W_c取值为零
														- ∂L/∂W_c = ∂L/∂z_c · h^T（链式法则）
															- ∂L/∂W_c = (∂L/∂z_c)·(∂z_c/∂W_c)：末端（矩阵求导链式法则）
															- ∂z_c/∂W_c = h^T：末端（线性变换矩阵求导规则）
														- ∂L/∂z_c取值为零：下方已分析
														- ∂L/∂z_c = p_c-1的计算结果在float32下为零
															- p_c在float32下计算为1.0：下方已分析
															- ∂L/∂z_c经链式法则推导为p_c-1
																- ∂L/∂z_c = ∂L/∂p_c·∂p_c/∂z_c
																	- ∂L/∂p_c = -1/p_c：末端（交叉熵损失L=-log(p_c)的导数）
																	- ∂p_c/∂z_c = p_c(1-p_c)：末端（softmax函数的偏导数性质）
																- p_c-1的计算结果为零：下方已分析
														- 正确类softmax值在float32下计算为1.0
															- softmax(z)_c计算结果为1.0
																- 其他各类exp贡献在float32加法中被舍入为零
																	- 各exp(z_j-z_c)项小于float32在1.0处的加法舍入阈值
																		- float32在1.0处的加法舍入阈值为2^(-24)
																			- float32在1.0处的ULP为2^(-23)
																				- float32使用23位尾数：末端（IEEE 754标准规定）
																			- float32加法采用就近舍入规则：末端（IEEE 754标准规定）
																				- 就近舍入下加法阈值为ULP的一半即2^(-24)：末端（IEEE 754标准规定）
																		- 对每个j≠c, exp(z_j-z_c) < 2^(-24)
																			- 对每个j≠c, z_c-z_j > 16.6
																				- 正确类logit值z_c取值大
																					- 模型训练已收敛至对训练样本的高分类准确率
																						- 训练交叉熵损失CE经多步梯度下降优化降至极低值
																							- CE取值趋近零：末端（数学定义）
																						- p_c对大多数训练样本接近1.0：上方已分析
																				- 各类别logit值z_j取值小
																					- 模型训练已收敛至对训练样本的高分类准确率：上方已分析

